# 07_SECURITY_TEST_MATRIX.md — Security & Vulnerability Test Matrix

This matrix governs all security testing, attack surface validation, secret protection, and input sanitization audits.

| Test ID | Feature ID | Attack / Risk | Input Payload / Test Vector | Expected Protection | Current Protection | Result Criteria |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `TEST-SEC-001` | `FEATURE-009` | SQL Injection Leakage | `admin' OR '1'='1` in raw query | Detect unparameterized SQL concatenation in source and API probes | Source AST scan flags raw SQL string template literals | Finding raised with severity `critical`, file & line located |
| `TEST-SEC-002` | `FEATURE-009` | Cross-Site Scripting (Reflected & Stored) | `<script>alert(1)</script>` in form input | Flag `dangerouslySetInnerHTML` and unescaped HTML interpolation | Source AST scan flags `dangerouslySetInnerHTML` | Finding raised with severity `high`, remediation guidance provided |
| `TEST-SEC-003` | `FEATURE-009` | Path Traversal Vulnerability | `../../../../etc/passwd` in filename input | Prohibit directory traversal across `WorkspaceGuard` | `WorkspaceGuard` rejects any path resolving outside root | Throws path violation error; file access blocked |
| `TEST-SEC-004` | `FEATURE-009` | Command Injection Vulnerability | `test; rm -rf /;` in CLI arguments | Strict parameterization in `ProcessRunner` without shell expansion | Parameterized execution in `child_process.spawn` | Subprocess executes safe binary arguments without command chaining |
| `TEST-SEC-005` | `FEATURE-009` | Secret & Token Leakage | JWT `eyJhbGciOi...` and `password: "secret123"` | Automatically redact secrets in test outputs, reports, and logs | `SecretRedactor` masks matching strings with `[REDACTED_JWT]` | Logs and reports contain zero exposed plaintext tokens/passwords |
| `TEST-SEC-006` | `FEATURE-009` | Safe Mode Execution Guard | Probe executed with `safeMode: true` on live URL | Never send destructive payloads (`DROP`, `DELETE ALL`, massive DoS) | `SecurityEngine` filters tests to `safeModeCompatible: true` | Probes remain completely non-destructive |
| `TEST-SEC-007` | `FEATURE-011` | SARIF Schema Conformance | Security report generated with 3 findings | Output compliant SARIF v2.1.0 JSON format for GitHub Security | `SarifExporterService` generates valid SARIF JSON | Schema matches `sarif-schema-2.1.0.json`; GitHub Code Scanning passes |
| `TEST-SEC-008` | `FEATURE-012` | Missing SRI Hashes on External Assets | `<script src="https://cdn.example.com/lib.js">` | Flag CDN resources loaded without `integrity` & `crossorigin` | `SriCsrfValidatorService` flags missing SRI attribute | Finding raised with severity `medium`, code fix provided |
| `TEST-SEC-009` | `FEATURE-012` | Unprotected Mutating Forms (Missing CSRF) | `<form method="POST">` without `_csrf` input | Flag mutating HTML forms lacking CSRF protection tokens | `SriCsrfValidatorService` flags mutating form elements | Finding raised with severity `medium`, CSRF template suggested |
| `TEST-SEC-010` | `FEATURE-012` | Dangerous Wildcard CORS Credentials | `Access-Control-Allow-Origin: *` with `credentials: true` | Flag insecure CORS configuration permitting cross-origin credential leak | `SriCsrfValidatorService` identifies wildcard origin with credentials | Finding raised with severity `high`, strict origin policy advised |
